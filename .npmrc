# Suppress audit warnings for build-time only vulnerabilities
# The 'tar' vulnerabilities are in node-gyp's build dependencies, not runtime code
# gl@8.1.6 is the latest version available and actively maintained
# These vulnerabilities only affect the build process, not the produced code
audit=false

